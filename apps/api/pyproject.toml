[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yazdan", email = "yazdan1382bab@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
api = "api:main"

[build-system]
requires = ["uv_build>=0.11.32,<0.12.0"]
build-backend = "uv_build"
